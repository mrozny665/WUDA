apiVersion: v1
kind: Service
metadata:
  name: {{ include "wuda-chart.fullname" . }}
  labels:
    {{- include "wuda-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wuda-chart.selectorLabels" . | nindent 4 }}
